// Library imports
import React from 'react';

// Hooks imports

// Styles imports
import styles from './newContactModal.module.scss';

// Components imports

// Context imports

const NewContactModal = () => {
	return (
		<div className={styles['newcontact-modal-wrapper']}>NewContactModal</div>
	);
};

export default NewContactModal;
